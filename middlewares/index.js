const {
  validationAddContact,
  validationUpdateContact,
} = require("./validations");
const ctrlWrapper = require("./ctrlWrapper");

module.exports = {
  validationUpdateContact,
  validationAddContact,
  ctrlWrapper,
};
